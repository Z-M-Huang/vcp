/**
 * Shim — preset utilities moved to @vcp-lib/llm-runner/presets in v0.6.0.
 *
 * The plugin keeps this file as a re-export so import paths like
 * `./preset-utils.ts` from sibling scripts and tests continue to resolve
 * without modification.
 */

export * from '@vcp-lib/llm-runner/presets';
