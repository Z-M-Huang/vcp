/**
 * Shim — stage type registry moved to @vcp-lib/prompt-assets/stage-definitions in v0.6.0.
 *
 * The plugin keeps this file as a re-export so import paths like
 * `../types/stage-definitions.ts` from dev-buddy code continue to resolve
 * without modification.
 */

export * from '@vcp-lib/prompt-assets/stage-definitions';
