/**
 * Shim — preset types moved to @vcp-lib/llm-runner/types in v0.6.0.
 *
 * The plugin keeps this file as a re-export so import paths like
 * `../types/presets.ts` from dev-buddy code continue to resolve without
 * modification.
 */

export * from '@vcp-lib/llm-runner/types';
